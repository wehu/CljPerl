#!/usr/bin/env perl

use strict;
use warnings;
use File::Basename;
use File::Spec;
use lib File::Spec->rel2abs(dirname(__FILE__) . "/../lib");

use CljPerl;

my $cljp = CljPerl::Eval->new();

foreach my $f (@ARGV) {
  $cljp->load($f);
}
