#!/usr/bin/env perl

use strict;
use warnings;
use File::Basename;
use File::Spec;
use lib File::Spec->rel2abs(dirname(__FILE__) . "/../lib");

use CljPerl;

my $cljp = CljPerl::Evaler->new();

$cljp->load("core");
foreach my $f (@ARGV) {
  $cljp->load($f);
}
